import random

frase = 'el perro de maria'
while True:
    letra = input('ingrese una letra: ')
    if letra in frase:
       lenght = len(frase)* '*' 
       for i,x enumerated frase:
           if frase==letra
           lenght[i]= x 
       print(lenght)
        
